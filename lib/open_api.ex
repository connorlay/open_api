defmodule OpenAPI do
  @moduledoc """
  OpenAPI is an Elixir parser and validator for [Open API 3.0.x](https://github.com/OAI/OpenAPI-Specification) documents.

  ## Design Goals

    [] Complete support for the Open API 3.0.x specification
    [] Extendable via `x-` style fields
  """

  def decode(input) do
  end

  def validate(document) do
  end
end
